namespaces:
    CommandSchedulerConstraints: Dukecity\CommandSchedulerBundle\Validator\Constraints\

Dukecity\CommandSchedulerBundle\Entity\ScheduledCommand:
    properties:
        cronExpression:
            - NotBlank: ~
            - CommandSchedulerConstraints:CronExpression: { message: "commandScheduler.validation.cron" }
        name:
            - NotBlank: ~
        command:
            - NotBlank: ~
        priority:
            - Type: { type: 'integer'}

